Update solpot tool using python


===========
🖥 Instructions: After downloading and unzipping, open terminal

1️⃣ pip install -r requirements.txt or pip3 install -r requirements.txt to install the required modules.

2️⃣ Input data setup:

data.txt stores cookie information. Format: solpotsid=s%3...

3️⃣ Run the tool with the command:

Windows or Termux: 
python run.py

MacOs or linux:
python3 run.py

==========
